import { defineCommand } from "citty";
export default defineCommand({ meta: { name: "validate", description: "Validate config (coming soon)" }, run() { console.log("validate not implemented yet"); } });
