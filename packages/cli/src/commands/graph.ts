import { defineCommand } from "citty";
export default defineCommand({ meta: { name: "graph", description: "Show dependency graph (coming soon)" }, run() { console.log("graph not implemented yet"); } });
